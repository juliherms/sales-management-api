version: '3.8'

services:
  mysql:
    image: mysql:8.0.23
    container_name: mysql
    ports: 
      - 3306:3306
    environment:
      MYSQL_USER: sales
      MYSQL_PASSWORD: sales
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sales_db
